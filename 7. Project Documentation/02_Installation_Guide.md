# Installation Guide

## Environment Setup
1. Create a Python virtual environment.
2. Install dependencies using `pip install -r requirements.txt`.
3. Activate the environment.

## Run the App
- Start the backend with Uvicorn.
- Start the frontend with Streamlit.
